replicaCount: 1

env:
  STORAGE_IN_MEMORY: "true"
  STORAGE_BACKEND: "memory"

dragonfly:
  enabled: false

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi
